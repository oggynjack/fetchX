services:
  fetchx:
    build: .
    image: fetchx:latest
    container_name: fetchx
    ports:
      - "8899:8899"
    volumes:
      - fetchx-downloads:/app/downloads
    restart: unless-stopped

volumes:
  fetchx-downloads:
